node_modules/
.DS_Store
*.log
/env.json
/.homeybuild/
/_stale_homeybuild*/

# Note: app.json is generated from .homeycompose/ on every `homey app validate`.
# It is committed alongside the source so CI can validate without a pre-compile step.
# Source of truth remains .homeycompose/app.json; do not hand-edit /app.json.

# Private working files (specs, screenshots, design iterations) not for public.
/private/

# Dev artefacts at the repo root (regeneratable).
/icon-preview.png
/flame-preview.png
/kettle-preview.png
